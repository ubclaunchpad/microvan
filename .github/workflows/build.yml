name: Build Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  python:
    uses: ubclaunchpad/microvan/.github/workflows/backend/python.yml
    secrets:
      django_secret: ${{ secrets.DJANGO_SECRET }}

